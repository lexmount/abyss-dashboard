interface:
  display_name: "Publish Abyss UI"
  short_description: "Release Abyss UI to npmjs with OIDC"
  default_prompt: "Use $publish-ui-package to publish the next @lexmount.com/abyss-ui release."
